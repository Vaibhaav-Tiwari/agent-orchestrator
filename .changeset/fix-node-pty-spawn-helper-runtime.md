---
"@aoagents/ao": patch
"@aoagents/ao-cli": patch
"@aoagents/ao-web": patch
---

Self-heal non-executable node-pty spawn-helper files at dashboard PTY spawn time and add an `ao doctor --fix` repair for installs where package lifecycle scripts were disabled.
